#include <stdio.h>

int main()
{
  int integer = 24;                              // integers are whole numbers, can be signed or unsigned
  float floatingp = 12.2;                        // decimals
  double doublef = 3.4;                          // decimals
  char alpha = 'A';                              // a single character in between single quotation marks
  char string[] = "This is a lot of characters"; // a string of characters "string" in other programming languages
  return 0;
}